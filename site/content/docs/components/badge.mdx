---
title: Badge
description: Badge component
---

import CountOutput from '../../../examples/badge/count.eg.astro';
import countCode from '../../../examples/badge/count.eg.astro?raw';
import GradientOutput from '../../../examples/badge/gradient.eg.astro';
import gradientCode from '../../../examples/badge/gradient.eg.astro?raw';
import HoverOutput from '../../../examples/badge/hover.eg.astro';
import hoverCode from '../../../examples/badge/hover.eg.astro?raw';
import IconOutput from '../../../examples/badge/icons.eg.astro';
import iconCode from '../../../examples/badge/icons.eg.astro?raw';
import PulseOutput from '../../../examples/badge/pulse.eg.astro';
import pulseCode from '../../../examples/badge/pulse.eg.astro?raw';
import ShapeOutput from '../../../examples/badge/shapes.eg.astro';
import shapeCode from '../../../examples/badge/shapes.eg.astro?raw';
import ShadowOutput from '../../../examples/badge/shadow.eg.astro';
import shadowCode from '../../../examples/badge/shadow.eg.astro?raw';
import SizesOutput from '../../../examples/badge/sizes.eg.astro';
import sizesCode from '../../../examples/badge/sizes.eg.astro?raw';
import StatusOutput from '../../../examples/badge/status.eg.astro';
import statusCode from '../../../examples/badge/status.eg.astro?raw';
import VariantsOutput from '../../../examples/badge/variants.eg.astro';
import variantsCode from '../../../examples/badge/variants.eg.astro?raw';
import ExampleBlock from '../../../playground/ExampleBlock.astro';

`Badge` is a compact UI component used to display labels, statuses, categories, counts, or other short pieces of information. The `Badge` component provides multiple variants and sizes while maintaining a consistent design across your application.

## Variants

Choose from a collection of predefined Badge variants to communicate different meanings or visual priorities.

The variants example demonstrates the available color styles provided by the Badge component. Use different variants to visually distinguish categories, actions, or semantic states while keeping the styling consistent across your application.

<ExampleBlock code={variantsCode}>
  <VariantsOutput />
</ExampleBlock>

## Sizes

Select an appropriate Badge size based on your interface.

The sizes example demonstrates the available Badge sizes. Use smaller badges for compact interfaces and larger sizes when the badge needs stronger visual emphasis.

<ExampleBlock code={sizesCode}>
  <SizesOutput />
</ExampleBlock>

## Shapes

Badges can use different border radii to match different design systems. Use pill-shaped badges for softer interfaces and square or sharp badges for more structured designs.

The shapes example demonstrates how a Badge can be customized with different border-radius styles. This is useful when matching the Badge to the visual language of your application.

<ExampleBlock code={shapeCode}>
  <ShapeOutput />
</ExampleBlock>

## Status

Combine a Badge with a status indicator to communicate states such as online, pending, processing, or offline.

The status example shows how Badges can be used to represent common application states. Pairing a status indicator with a Badge makes the state easier to identify at a glance.

<ExampleBlock code={statusCode}>
  <StatusOutput />
</ExampleBlock>

## Icons

Badges can include icons to provide additional visual context.

The icons example demonstrates how icons can be placed alongside Badge text. Icons are useful when additional visual context is needed, such as identifying a feature, category, or status.

<ExampleBlock code={iconCode}>
  <IconOutput />
</ExampleBlock>

## Hover

Use hover transitions when a Badge is interactive.

The hover example demonstrates how transition and hover utilities can provide visual feedback when users interact with a Badge. This is particularly useful when the Badge is used as a link or interactive element.

<ExampleBlock code={hoverCode}>
  <HoverOutput />
</ExampleBlock>

## Shadows

Add subtle shadows to create additional visual separation from the surrounding content.

The shadows example demonstrates how shadow utilities can be combined with a Badge to make it stand out from the surrounding interface. Use subtle shadows when additional depth or separation is needed.

<ExampleBlock code={shadowCode}>
  <ShadowOutput />
</ExampleBlock>

## Gradient

Badges can also use gradient backgrounds for more expressive visual treatments.

The gradient example demonstrates how a Badge can be styled with a gradient background. Gradient styles can be useful for promotional labels, featured content, or other situations where a stronger visual treatment is appropriate.

<ExampleBlock code={gradientCode}>
  <GradientOutput />
</ExampleBlock>

## Count

Use compact Badges to display notification or message counts.

The count example demonstrates how Badges can be used to display numerical information such as unread notifications, messages, alerts, or item counts. Keep count Badges compact so the value remains easy to scan.

<ExampleBlock code={countCode}>
  <CountOutput />
</ExampleBlock>

## Pulse

Animated indicators can be used for live or active states.

The pulse example demonstrates how an animated indicator can be combined with a Badge to communicate live, active, or changing states. Use animations sparingly so they attract attention without becoming distracting.

<ExampleBlock code={pulseCode}>
  <PulseOutput />
</ExampleBlock>
